package metla

/*import (
	"bytes"
	"fmt"
)

func init() {
	creators = append(creators, &valueCreator{
		checker:     checkValFor,
		constructor: newValFor,
	})
}

func checkValFor(src []byte) bool {
	fmt.Println("FOR_CHECK", string(src))
	return bytes.Index(src, []byte("for")) == 0
}

func newValFor(p *parser) (res token, err error) {
	fmt.Println("|||", string(p.EndLineContent()))
	err = fmt.Errorf("For Test Error...")
	return
}*/

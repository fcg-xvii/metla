{%
	methods = [
		{ name: "listLibraries", shortDescription: "Список библиотек"
			url: "https://www.admefine.ru/api/dictionary/listLibraries"
			params: []
		}
		{ 
			name: "searchRecordEntry"
			shortDescription: "Поиск объектов по вхождению указанной строки (пустая строка - любые вхождения)"
			url: "https://www.admefine.ru/api/dictionary/searchRecordEntry"
			params: [
				{
					name: "entry"
					type: "string"
					necessary: true
					description: "Вхождение. Пустая строка - все вхождения"
				}, {
					name: "library"
					type: "integer, string"
					necessary: false
					description: "Идентификатор (или наименование) библиотеки, в которой производим поиск. Пустое значение - поиск по всем библиотекам"
				}, {
					name: "parentItem"
					type: "integer, string"
					necessary: false
					description: "vv"
				}
			]
		}
	]
%}

{% range i in 0:len(methods) %}
	<div class="d-flex flex-row ml-3 mt-3">
		<div class="p-0 top-method-name">{{ methods[i].name }}</div>
		{* <div class="p-0 short-params">{% include "/include/short-params.tpl" { list: methods[i].params } %}</div> *}
	</div>
	<div class="d-flex flex-row ml-3 mt-0">
		<div class="p-0 top-method-url">{{ methods[i].url }}</div>
	</div>
		<div class="d-flex flex-row ml-3 mt-0">
			<div class="p-0 top-method-description">{{ methods[i].shortDescription }}</div>
		</div>
		<div class="d-flex flex-row ml-3 mt-0">
			<div class="p-0 method-description">{{ methods[i].description }}</div>
		</div>
		{% if len(methods[i].params) > 0 %}
		<div class="d-flex flex-row ml-3 mt-0">
			<div class="p-0 method-description">Описания входных параметров</div>
			{% each index, val -> methods[i].params %}
				<div class="p-2">
					{{ index }}: {{ val }}
				</div>
			{% endfor %}
		</div>
		{% endif %}
	<hr>
{% endfor %}
